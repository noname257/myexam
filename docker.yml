---
  - hosts: all
    become: yes
    tasks:
      - name: copy script to server
        copy:
          src: /etc/ansible/script/telebot.sh
          dest: /home/telebot.sh
          mode: u=rwx,g=rx,o=rx


      - name: Run installation script
        shell: /home/telebot.sh
